<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta http-equiv="X-UA-Compatible" content="ie=edge">
        <title>Hoboken Curbside</title>
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.4.1/dist/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.2/font/bootstrap-icons.css">
        {{!-- Uncomment the line below when the branch handling the CSS is merged. --}}
        <link rel="stylesheet" href="../../public/css/main.css">
    </head>
    <body>
        <header class="mb-3">
            {{!-- If we're on the "Account Management" page, then display the corresponding 
            navigation bar. --}}
            {{#if this.context.mgmtPage}}
                {{> mgmtNav this.context }}
            {{!-- Else, display the default navigation bar. --}}
            {{else}}
                {{> nav this.context }}
            {{/if}}
        </header>

        <main class="main-container">
            {{!-- Main page content goes here. --}}
            <div class="container">
                {{{body}}}
            </div>
        </main>

        {{!-- Note for whoever's tackling the CSS: you should probably make the footer fixed to the 
        bottom if the screen size for laptops or larger (no mobile/tablet). - Chance --}}
        <footer class="mt-3">
            {{!-- Depending on which page we're on, we'll use the 'pagination' partial to 
            render the appropriate footer. --}}
            <div class="card mw-100 text-center">
                <div class="card-body">
                    {{> pagination this.context }}
                </div>
            </div>
        </footer>

        {{!-- Include the jQuery and Bootstrap scripts. --}}
        <script src="https://code.jquery.com/jquery-3.2.1.min.js" integrity="sha384-xBuQ/xzmlsLoJpyjoggmTEz8OWUFM0/RC5BsqQBDX2v5cMvDHcMakNTNrHIW2I5f" crossorigin="anonymous"></script>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-Fy6S3B9q64WdZWQUiU+q4/2Lc9npb8tCaSX9FK7E8HnRr0Jz8D6OP9dO5Vg3Q9ct" crossorigin="anonymous"></script>
        {{!-- Incorporate the script partials here. --}}
        {{#unless this.context.noPagination}}
            <script src="https://pagination.js.org/dist/2.4.1/pagination.min.js"></script>
        {{/unless}}
        {{#if scripts}}
            {{#each scripts}}
                <script src="{{this}}"></script>
            {{/each}}
        {{/if}}
    </body>
</html>